/** Constantes partagées (préférences produit, pas les secrets). */

/** Quota affiché direction / administration pour le stockage Cloud (bucket documents). 100 Gio (base binaire). */
export const CLOUD_STORAGE_QUOTA_BYTES = 100 * 1024 ** 3;
