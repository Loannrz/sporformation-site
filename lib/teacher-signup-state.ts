export type TeacherSignupFormState = {
  errorCode: string | null;
  devDetail: string | null;
};

export const initialTeacherSignupState: TeacherSignupFormState = {
  errorCode: null,
  devDetail: null,
};
